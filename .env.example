# AirCode 环境变量：仅保留服务端口与应用 Home
# 其它（API Key、模型、项目目录等）一律写入 SQLite（CLAUDE_HOME/settings.db）

# 服务端口（默认 10300）
PORT=10300

# 应用 Home / Claude Home（默认 ~/.aircode）
# 内含 settings.db 与 Claude Code 配置目录 claude/
# CLAUDE_HOME=/path/to/aircode-home
